# ========================================
# PostgreSQL - Configuração Segura
# ========================================
# IMPORTANTE: Copie para .env e configure com valores reais
# NUNCA commitar .env no Git!

# ========== CREDENCIAIS DO BANCO ==========
# Usuário administrador do PostgreSQL
DB_USER=postgres_admin

# Senha forte (mínimo 32 caracteres, com maiúsculas, minúsculas, números e símbolos)
# Gere com: python -c "import secrets, string; chars = string.ascii_letters + string.digits + string.punctuation; print(''.join(secrets.choice(chars) for _ in range(32)))"
DB_PASSWORD=GERAR_SENHA_FORTE_AQUI_32_CARACTERES

# Nome do banco de dados
DB_NAME=bot_whatsapp_prod

# Porta (apenas localhost - não expor publicamente)
DB_PORT=5432

# ========== BACKUP ==========
# Senha para criptografar backups
# Gere com: python -c "import secrets; print(secrets.token_urlsafe(32))"
BACKUP_PASSWORD=GERAR_SENHA_BACKUP_AQUI

# Dias de retenção de backup
BACKUP_RETENTION_DAYS=30

# ========== MONITORAMENTO (Opcional) ==========
GRAFANA_PASSWORD=GERAR_SENHA_GRAFANA_AQUI

# ========== NOTAS DE SEGURANÇA ==========
# 1. Use senhas diferentes para cada ambiente (dev/staging/prod)
# 2. Rotacione senhas a cada 90 dias
# 3. Nunca use senhas padrão como "senha123"
# 4. Mantenha este arquivo em .gitignore
